import { ButtonIcons } from "./types";
import { PlusCircleIcon } from "assets/svgs";
import { ReactNode } from "react";

export const BUTTONS_ICONS: Record<ButtonIcons, ReactNode> = {
  plusCircle: <PlusCircleIcon />,
};
